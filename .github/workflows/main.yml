name: Run tests
on:
  workflow_dispatch:
  pull_request:
    branches: [ master ]
    paths: 
      - 'src/**/*'
      - 'test/**/*'
jobs:
  tests:
    runs-on: ubuntu-22.04
    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: Run tests
        run: dotnet test
